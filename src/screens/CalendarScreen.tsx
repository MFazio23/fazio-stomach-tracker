import {Box, Typography} from '@mui/material';

export function CalendarScreen() {
    return (
        <Box>
            <Typography variant="h2">Calendar</Typography>
            <Typography variant="body1">
                This will eventually have a calendar with some kind of summary.
                Or else I'll delete the tab, who can say?
                Or maybe we can just have this show charts instead, that may be good.
            </Typography>
            <Typography variant="h1">🤷🏻‍♀️</Typography>
        </Box>
    )
}
